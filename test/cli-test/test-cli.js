#!/usr/bin/env node

// Import the CLI script directly
import "../../bin/cli.js";

// This is just a wrapper to run the CLI script for testing
// No additional code needed as we're directly importing the CLI script
